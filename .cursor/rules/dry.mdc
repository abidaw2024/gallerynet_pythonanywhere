---
description: 
globs: 
alwaysApply: true
---
# ♻️ Principio DRY (Don't Repeat Yourself)

Evita repetir código o lógica en múltiples lugares. Si detectas duplicación, refactoriza para extraer esa lógica en funciones reutilizables, hooks, utilidades o componentes.

### 🔁 ¿Qué considerar para aplicar DRY?

1. **Funciones repetidas:**
   Si ves bloques similares en diferentes funciones, unifícalos en una sola función reutilizable.

2. **Lógica duplicada en clases o componentes:**
   Extrae métodos comunes a una clase base o crea un helper en un archivo compartido.

3. **Código similar en múltiples archivos:**
   Evalúa si se puede reutilizar creando un módulo o archivo compartido (`utils`, `hooks`, `services`, etc.).

4. **Validaciones repetidas:**
   Centraliza las validaciones en funciones únicas. Ej: `validateEmail()`, `isEmpty()`, etc.

5. **Hooks duplicados en React:**
   Si dos componentes usan el mismo estado y efecto, crea un `custom hook`.

6. **Constantes o valores mágicos:**
   Declara valores compartidos (como strings, rutas, números clave) en un solo archivo.

---

### ✅ Buenas prácticas para aplicar DRY:
- Refactoriza pequeños bloques duplicados, no esperes a que el problema escale.
- Dale nombres claros y semánticos a funciones reutilizadas.
- No sobreapliques DRY si la lógica aún no está madura. A veces es mejor duplicar primero y abstraer después.
- Usa el principio **"Abstrae cuando haya al menos 3 repeticiones"** como guía.

---

Aplica esta regla cada vez que copies y pegues código, o veas patrones repetitivos en funciones, clases o componentes.
